# 分析: 项目进度与全量未完成任务梳理 (2026-04-12)

**文档背景**: 
基于用户提出的要求，需要从项目文档库（特别是 `docs/` 目录以及根目录）中整理出目前尚未完成的任务，作为后续开发和追踪的基准参考。

---

### 1. 我实际检查了哪些内容
- `docs/2026-04-12-15-25-计划-全量待办任务汇总.md` (当前最新的主计划文档)
- 根目录 `TODO.md`
- `docs/TODO.md` (早期历史计划文档)
- `docs/logs/` 目录下的多份近期开发日志（如 04-08、04-10 的日志）
- 对整个工作区使用 `grep` 检索了所有未勾选的 `- [ ]` 任务项。

### 2. 已确认的事实
根据以上材料的交叉比对，核心工具和统一输出协议已闭环，以下是明确尚未完成的待办任务：

**性能与架构优化 (Token 压缩与 CLI 增强)**
- [ ] **实现 `batch_ops.py`**：支持通过单个 JSON 或文件一次性执行多个原子操作，实现单次 Git Commit 汇总。
- [ ] **精简 XML 冗余字段**：实施极致的 Token 压缩方案。
- [ ] **生成 ZSH/Bash 动态补全**：为 `memocli` 生成基于库内容（类型、实体名）的补全脚本。
- [ ] **Token 压缩比对与压力测试**：实测全量协议适配后，统一协议及帮助系统增强带来的 Token 消耗降低比例。

**安全机制与 Agent 规范同步**
- [ ] **重写 `SKILL.md`**：强制引导 Agent 优先使用安全无冲突的 `append-update` + `consolidate` (追加并整理) 模式。
- [ ] **工具风险降级标注**：正式标记 `replaceSection` 等直接修改正文的工具为“高风险/不推荐”。

**高级工具补全 (遗留的 Tools-Spec)**
- [ ] **检索增强**：实现 `findEntitiesByMetadata` (基于 YAML 属性的精确/范围过滤)，以及 `searchEntitiesGlobally` (整合名称与正文的混合检索，适配 XML 结果列表)。
- [ ] **系统维护**：实现 `backupLibrary` (基于 Git Branch 或压缩包的库备份逻辑)，以及 `garbageCollectRelations` (自动扫描并清理失效的关系引用)。

**质量保证与深度鲁棒性**
- [ ] **补全 Orphan Check**：在 `manage_relations` 中补全孤儿引用检查的拦截逻辑。
- [ ] **模板生成优化**：针对高频任务（如实体创建），优化 `schema_define.py` 中的模板生成逻辑。
- [ ] **集成测试**：运行全量集成测试，验证 `memocli` 在多子命令切换时的鲁棒性。

### 3. 目前不能确认的部分
- **命名规范迁移的真实进度**：04-10 的开发日志声称“编写并执行了”实体命名标准化迁移（剥离前缀等），但 04-12 的最新汇总计划以及根目录 `TODO.md` 仍将其标记为“待执行全库清理/清零历史包袱”。不足以确认全库文件是否真的已经完成了批量重命名。
- **旧版 TypeScript 开发计划是否完全废弃**：`docs/TODO.md` 中仍留有大量基于 TypeScript 的“编辑器逻辑”和“工具集”开发未完成，但项目实际重心已转移至 Python 脚本 (`memocli`)。不能确认这些旧任务是否仍需保留。

### 4. 我对项目状态的判断（保守表述）
- 项目的底层运行架构（Python CLI 工具链、XML 报告、错误处理机制）已具备基本可用性。
- 项目当前处于从“基础可用”向“高效低耗（控制 Token）”、“高安全（强制缓冲区编辑）”演进的过渡阶段。
- 目前多份进度文档（根目录 TODO、docs/TODO、各类计划文档）之间存在信息割裂和状态不一致，这可能导致后续任务推进时的方向混乱。

### 5. 更好的下一步建议
- **统一进度源**：建议以本篇文档或 `2026-04-12-15-25-计划-全量待办任务汇总.md` 为唯一基准，归档或废弃陈旧的 `docs/TODO.md`。
- **安全优先**：在开发新工具之前，建议优先完成 `SKILL.md` 的重写和工具风险标注，以确保外部 Agent 调用时的安全性。
- **状态对齐**：人工核对一下 `assets/template-library/entities/` 中的实体文件名，以敲定命名迁移任务是否可以正式划线勾销。

### 6. 如果需要执行高风险动作，先给 dry-run / preview 方案
- 如果需要继续执行**全库命名迁移清理**：
  - 建议先执行 `python memories-off-skill/scripts/migration_naming_v2.py --dry-run`。
  - 预览输出日志中列出的即将被重命名的文件列表以及对应的 WikiLink 修复差异，确认无误后再移除 `--dry-run` 标志执行。
- 如果需要**废弃陈旧待办文档**：
  - 不要直接 `rm` 删除，建议执行 `git mv docs/TODO.md legacy/TODO.md` 将其移入遗留归档目录，保留项目演进的历史上下文。