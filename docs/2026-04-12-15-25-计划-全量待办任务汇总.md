# 计划: 全量待办任务汇总 (Master-TODO) - 2026-04-12 校准版

## 1. 状态盘点 (Current Audit)
经过 2026-04-12 15:40 的深度代码核查，项目实际进度显著领先于文档。底层架构（ScriptBase 协议、XML 报告、错误分流）已全量铺开，核心迁移与整理脚本已就绪。

---

## 2. 核心架构与协议优化 (Phase 2)

### 2.1 [已完成] 统一输出协议适配 (Unified-Protocol)
- [x] **ScriptBase 深度重构**：`schema_define.py` 已实现 `finalize()`，接管 `stdout` 输出标准 XML。
- [x] **全量脚本适配**：`search.py`, `stats.py`, `create_entity.py`, `append_update.py` 等 17 个脚本已全部继承 `ScriptBase`。
- [x] **错误分流实现**：`CustomArgumentParser` 已实现 `stderr` 输出信号、`stdout` 输出 XML 上下文。

### 2.2 [已完成] 帮助系统语义化增强
- [x] **枚举详情注入**：`ScriptBase` 适配 `enum_details`。
- [x] **全局帮助一站式展示**：`install.py` 已能自动抓取 Example 和 Enum 细节。

### 2.3 [待办] 性能极致优化
- [ ] **实现 `batch_ops.py`**：支持通过单个 JSON/文件一次性执行多个原子操作，实现单次 Git Commit 汇总。
- [ ] **ZSH/Bash 动态补全**：为 `memocli` 生成基于库内容（类型、实体名）的补全脚本。

---

## 3. 安全编辑与梦境整理 (Buffer-then-Dreaming)

### 3.1 [已完成] 缓冲区机制闭环
- [x] **实现 `append_update.py`**：向文件末尾追加更新块。
- [x] **实现 `consolidate_updates.py`**：执行“梦境整理”算法，将更新块安全合入正文。

### 3.2 [待办] 规范与准则同步
- [ ] **重写 `SKILL.md`**：强制引导 Agent 优先使用 `append-update` + `consolidate` 模式。
- [ ] **工具降级标注**：正式标记 `replaceSection` 等工具为“高风险/不推荐”。

---

## 4. 库维护与关系管理 (Structure-Maintenance)

### 4.1 [已就绪/待执行] 实体命名标准化迁移 (Naming-Migration)
- [x] **实现 `migration_naming_v2.py`**：支持 Dry Run、Git MV、WikiLink 修复及元数据关系修复。
- [ ] **执行全库清理**：择机运行此脚本清零历史包袱。

### 4.2 [已完成/待补全] 关系与类型自动化
- [x] **实现 `manage_relations.py`**：支持 `--add` 和 `--delete`。
- [x] **实现类型管理工具**：`rename_type.py`, `merge_types.py` 已就绪。
- [ ] **关系完整性增强**：在 `manage_relations` 中补全 Orphan check（孤儿引用检查）的拦截逻辑。

---

## 5. 遗留功能补全 (From Tools-Spec)

### 5.1 检索类增强
- [ ] **实现 `findEntitiesByMetadata`**：基于 YAML 属性的精确/范围过滤。
- [ ] **实现 `searchEntitiesGlobally`**：整合名称与正文的混合检索，适配 XML 结果列表。

### 5.2 系统维护类
- [ ] **实现 `backupLibrary`**：基于 Git Branch 或压缩包的库备份逻辑。
- [ ] **实现 `garbageCollectRelations`**：自动扫描并清理失效的关系引用。

---

## 6. 验证与交付
- [ ] **Token 压缩比对**：实测优化前后的 Token 消耗（特别是全局帮助增强后的单次交互收益）。

---
**核查依据：**
- `memories-off-skill/scripts/schema_define.py` (ScriptBase & Error Handling)
- `memories-off-skill/scripts/migration_naming_v2.py` (Full Feature)
- `memories-off-skill/scripts/consolidate_updates.py` (Dreaming Engine)
- 全量 18 个 Python 脚本的 `grep` 扫描结果。
