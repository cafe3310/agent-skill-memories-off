# 开发日志：graph.py 模板拆分与工程化改进 (2026-04-08)

## 1. 概述 (Overview)
为了提升可视化工具的可维护性和前端代码的开发体验，本任务对 `graph.py` 进行了重构，将其内置的大型 HTML/JS 模板提取到了独立的外部文件中。

## 2. 变更详情 (Actions)

### 2.1 资源分离 (Resource Separation)
- **新建文件**: `memories-off-skill/assets/graph_template.html`
- **操作**: 
    - 使用 shell 命令（`sed`）将 `graph.py` 中的硬编码 HTML 字符串精准提取。
    - 确保了 `{DATA_JSON}` 占位符在提取过程中完整保留，以供后续的数据注入。

### 2.2 逻辑优化 (Logic Refinement)
- **修改文件**: `memories-off-skill/scripts/graph.py`
- **重构内容**: 
    - 移除了 100 余行的长字符串常量。
    - 引入了基于 `Path(__file__).parent` 的动态路径查找逻辑，确保在不同安装环境下均能正确加载 assets 资源。
    - 增加了文件加载的异常处理机制（备退机制），提升了脚本的健壮性。

## 3. 改进效益 (Benefits)
1. **开发者友好**: 现在可以利用 IDE 对 HTML/JS 的原生支持（高亮、补全、格式化）直接维护图谱前端。
2. **代码精简**: Python 脚本主体逻辑更加聚焦于数据提取与导出，体积大幅缩小。
3. **工程化**: 确立了 `assets/` 目录作为技能附属静态资源的存放规范。

## 4. 验证状态 (Validation)
- [x] 运行 `memocli graph`，脚本成功从 assets 加载模板并正确注入 JSON 数据。
- [x] 浏览器正常唤起，3D 引力图谱交互功能完好。
